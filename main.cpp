#include <sten/tensor.hpp>

#include <unistd.h>
#include <iostream>
#include <numeric>
#include <vector>

int main()
{
  int n = 700;
  // sten::tensor t1({n, n, n}, sten::kCPU);
  // sten::tensor t2({n, n, n}, sten::kCPU);

  // auto t3 = t1 + t2;
  // return (int)t3.get_at(0);
}
